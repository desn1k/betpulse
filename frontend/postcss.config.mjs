// Tailwind CSS v4 is configured entirely in CSS (see app/globals.css); the
// PostCSS plugin is all the build needs.
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
